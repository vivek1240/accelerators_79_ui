# Copy to .env and set for your environment.
# Frontend uses the Node backend (mvp_demo_backend) for auth + proxy to FastAPI.

# Local dev (Node backend on port 4000):
# VITE_API_BASE=http://localhost:4000

# Production (deployed Node backend):
# VITE_API_BASE=https://your-mvp-demo-backend.up.railway.app

# Optional: when using Vite dev server with /api proxy, proxy target (default 4000):
# VITE_API_PROXY_TARGET=http://localhost:4000
